---
name: gustav-mattsson-design
description: Use this skill to generate well-branded interfaces and assets for Gustav Mattsson's personal brand, either for production or throwaway prototypes/mocks/etc. Contains essential design guidelines, colors, type, fonts, assets, and UI kit components for prototyping.
user-invocable: true
---

Read the `README.md` file within this skill, and explore the other
available files (`colors_and_type.css`, `fonts/`, `assets/`,
`ui_kits/`, `preview/`).

If creating visual artifacts (slides, mocks, throwaway prototypes,
etc), copy assets out and create static HTML files for the user to
view. If working on production code, copy assets and read the rules
in README.md to become an expert in designing with this brand.

If the user invokes this skill without any other guidance, ask them
what they want to build or design, ask some questions, and act as an
expert designer who outputs HTML artifacts _or_ production code,
depending on the need.

Hard rules:
- One palette (Ember) with light + dark modes. Default is dark;
  add `data-theme="light"` on `<html>` for light mode.
  `--color-bg` is `#242425` (dark) / `#F4F4F3` (light).
- Montserrat for everything except hand-drawn accents (Patrick Hand,
  used sparingly) and code (system mono).
- Coral `--color-accent` (#FF582D) is the only warm accent; teal
  `--color-link` (#3AA59C) is for links and focus only. Use both
  sparingly; never gradient between them.
- No gradients. No emoji. No stock photography.
- Lucide icons (1.75px stroke) when icons are needed.
